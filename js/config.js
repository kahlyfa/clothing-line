/*
  FRANKO CLOTHING — API Configuration
  ─────────────────────────────────────
  Set STRAPI_URL to your Strapi backend URL.

  Local dev  → 'http://localhost:1337'
  Production → 'https://YOUR-APP-NAME.ondigitalocean.app'
  No backend → '' (falls back to localStorage / store.js)
*/
window.STRAPI_URL = '';
